# Results

This folder stores benchmark outputs.

- `raw/`: raw JSON/CSV/log files generated by benchmark commands
- `processed/`: cleaned tables and derived metrics

Large result files should usually stay out of Git unless they are small enough to review easily.